// Code generated by enthistory, DO NOT EDIT.
package schema

import (
	"entgo.io/ent"
	"entgo.io/ent/dialect/entsql"
	"entgo.io/ent/schema"
	"entgo.io/ent/schema/field"

	"github.com/flume/enthistory"

	"time"
)

// QuestionHistory holds the schema definition for the QuestionHistory entity.
type QuestionHistory struct {
	ent.Schema
}

// Annotations of the QuestionHistory.
func (QuestionHistory) Annotations() []schema.Annotation {
	return []schema.Annotation{
		entsql.Annotation{
			Table: "question_history",
		},
        enthistory.Annotations{
            IsHistory: true,
            Exclude: true,
        },
	}
}

// Fields of the QuestionHistory.
func (QuestionHistory) Fields() []ent.Field {
	historyFields := []ent.Field{
        field.Time("history_time").
            Default(time.Now).
            Immutable(),
        field.Int("ref").
            Immutable().
            Optional(),
        field.Enum("operation").
            GoType(enthistory.OpType("")).
            Immutable(),
        field.Int("updated_by").
            Optional().
            Immutable().
            Nillable(),
    }


    original := Question{}
    for _, field := range original.Fields() {
        if field.Descriptor().Name != "id" {
            historyFields = append(historyFields, field)
        }
    }

    return historyFields
}

// Mixin of the QuestionHistory.
func (QuestionHistory) Mixin() []ent.Mixin {
	return Question{}.Mixin()
}
