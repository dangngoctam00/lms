// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"lms-class/ent/examhistory"
	"lms-class/ent/predicate"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ExamHistoryQuery is the builder for querying ExamHistory entities.
type ExamHistoryQuery struct {
	config
	ctx        *QueryContext
	order      []examhistory.OrderOption
	inters     []Interceptor
	predicates []predicate.ExamHistory
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExamHistoryQuery builder.
func (ehq *ExamHistoryQuery) Where(ps ...predicate.ExamHistory) *ExamHistoryQuery {
	ehq.predicates = append(ehq.predicates, ps...)
	return ehq
}

// Limit the number of records to be returned by this query.
func (ehq *ExamHistoryQuery) Limit(limit int) *ExamHistoryQuery {
	ehq.ctx.Limit = &limit
	return ehq
}

// Offset to start from.
func (ehq *ExamHistoryQuery) Offset(offset int) *ExamHistoryQuery {
	ehq.ctx.Offset = &offset
	return ehq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ehq *ExamHistoryQuery) Unique(unique bool) *ExamHistoryQuery {
	ehq.ctx.Unique = &unique
	return ehq
}

// Order specifies how the records should be ordered.
func (ehq *ExamHistoryQuery) Order(o ...examhistory.OrderOption) *ExamHistoryQuery {
	ehq.order = append(ehq.order, o...)
	return ehq
}

// First returns the first ExamHistory entity from the query.
// Returns a *NotFoundError when no ExamHistory was found.
func (ehq *ExamHistoryQuery) First(ctx context.Context) (*ExamHistory, error) {
	nodes, err := ehq.Limit(1).All(setContextOp(ctx, ehq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{examhistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ehq *ExamHistoryQuery) FirstX(ctx context.Context) *ExamHistory {
	node, err := ehq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ExamHistory ID from the query.
// Returns a *NotFoundError when no ExamHistory ID was found.
func (ehq *ExamHistoryQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ehq.Limit(1).IDs(setContextOp(ctx, ehq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{examhistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ehq *ExamHistoryQuery) FirstIDX(ctx context.Context) int {
	id, err := ehq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ExamHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ExamHistory entity is found.
// Returns a *NotFoundError when no ExamHistory entities are found.
func (ehq *ExamHistoryQuery) Only(ctx context.Context) (*ExamHistory, error) {
	nodes, err := ehq.Limit(2).All(setContextOp(ctx, ehq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{examhistory.Label}
	default:
		return nil, &NotSingularError{examhistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ehq *ExamHistoryQuery) OnlyX(ctx context.Context) *ExamHistory {
	node, err := ehq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ExamHistory ID in the query.
// Returns a *NotSingularError when more than one ExamHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (ehq *ExamHistoryQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ehq.Limit(2).IDs(setContextOp(ctx, ehq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{examhistory.Label}
	default:
		err = &NotSingularError{examhistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ehq *ExamHistoryQuery) OnlyIDX(ctx context.Context) int {
	id, err := ehq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ExamHistories.
func (ehq *ExamHistoryQuery) All(ctx context.Context) ([]*ExamHistory, error) {
	ctx = setContextOp(ctx, ehq.ctx, "All")
	if err := ehq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ExamHistory, *ExamHistoryQuery]()
	return withInterceptors[[]*ExamHistory](ctx, ehq, qr, ehq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ehq *ExamHistoryQuery) AllX(ctx context.Context) []*ExamHistory {
	nodes, err := ehq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ExamHistory IDs.
func (ehq *ExamHistoryQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ehq.ctx.Unique == nil && ehq.path != nil {
		ehq.Unique(true)
	}
	ctx = setContextOp(ctx, ehq.ctx, "IDs")
	if err = ehq.Select(examhistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ehq *ExamHistoryQuery) IDsX(ctx context.Context) []int {
	ids, err := ehq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ehq *ExamHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ehq.ctx, "Count")
	if err := ehq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ehq, querierCount[*ExamHistoryQuery](), ehq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ehq *ExamHistoryQuery) CountX(ctx context.Context) int {
	count, err := ehq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ehq *ExamHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ehq.ctx, "Exist")
	switch _, err := ehq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ehq *ExamHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := ehq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExamHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ehq *ExamHistoryQuery) Clone() *ExamHistoryQuery {
	if ehq == nil {
		return nil
	}
	return &ExamHistoryQuery{
		config:     ehq.config,
		ctx:        ehq.ctx.Clone(),
		order:      append([]examhistory.OrderOption{}, ehq.order...),
		inters:     append([]Interceptor{}, ehq.inters...),
		predicates: append([]predicate.ExamHistory{}, ehq.predicates...),
		// clone intermediate query.
		sql:  ehq.sql.Clone(),
		path: ehq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		HistoryTime time.Time `json:"history_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ExamHistory.Query().
//		GroupBy(examhistory.FieldHistoryTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ehq *ExamHistoryQuery) GroupBy(field string, fields ...string) *ExamHistoryGroupBy {
	ehq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExamHistoryGroupBy{build: ehq}
	grbuild.flds = &ehq.ctx.Fields
	grbuild.label = examhistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		HistoryTime time.Time `json:"history_time,omitempty"`
//	}
//
//	client.ExamHistory.Query().
//		Select(examhistory.FieldHistoryTime).
//		Scan(ctx, &v)
func (ehq *ExamHistoryQuery) Select(fields ...string) *ExamHistorySelect {
	ehq.ctx.Fields = append(ehq.ctx.Fields, fields...)
	sbuild := &ExamHistorySelect{ExamHistoryQuery: ehq}
	sbuild.label = examhistory.Label
	sbuild.flds, sbuild.scan = &ehq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExamHistorySelect configured with the given aggregations.
func (ehq *ExamHistoryQuery) Aggregate(fns ...AggregateFunc) *ExamHistorySelect {
	return ehq.Select().Aggregate(fns...)
}

func (ehq *ExamHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ehq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ehq); err != nil {
				return err
			}
		}
	}
	for _, f := range ehq.ctx.Fields {
		if !examhistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ehq.path != nil {
		prev, err := ehq.path(ctx)
		if err != nil {
			return err
		}
		ehq.sql = prev
	}
	return nil
}

func (ehq *ExamHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ExamHistory, error) {
	var (
		nodes = []*ExamHistory{}
		_spec = ehq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ExamHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ExamHistory{config: ehq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(ehq.modifiers) > 0 {
		_spec.Modifiers = ehq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ehq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ehq *ExamHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ehq.querySpec()
	if len(ehq.modifiers) > 0 {
		_spec.Modifiers = ehq.modifiers
	}
	_spec.Node.Columns = ehq.ctx.Fields
	if len(ehq.ctx.Fields) > 0 {
		_spec.Unique = ehq.ctx.Unique != nil && *ehq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ehq.driver, _spec)
}

func (ehq *ExamHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(examhistory.Table, examhistory.Columns, sqlgraph.NewFieldSpec(examhistory.FieldID, field.TypeInt))
	_spec.From = ehq.sql
	if unique := ehq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ehq.path != nil {
		_spec.Unique = true
	}
	if fields := ehq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, examhistory.FieldID)
		for i := range fields {
			if fields[i] != examhistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ehq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ehq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ehq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ehq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ehq *ExamHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ehq.driver.Dialect())
	t1 := builder.Table(examhistory.Table)
	columns := ehq.ctx.Fields
	if len(columns) == 0 {
		columns = examhistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ehq.sql != nil {
		selector = ehq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ehq.ctx.Unique != nil && *ehq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ehq.modifiers {
		m(selector)
	}
	for _, p := range ehq.predicates {
		p(selector)
	}
	for _, p := range ehq.order {
		p(selector)
	}
	if offset := ehq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ehq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (ehq *ExamHistoryQuery) ForUpdate(opts ...sql.LockOption) *ExamHistoryQuery {
	if ehq.driver.Dialect() == dialect.Postgres {
		ehq.Unique(false)
	}
	ehq.modifiers = append(ehq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return ehq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (ehq *ExamHistoryQuery) ForShare(opts ...sql.LockOption) *ExamHistoryQuery {
	if ehq.driver.Dialect() == dialect.Postgres {
		ehq.Unique(false)
	}
	ehq.modifiers = append(ehq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return ehq
}

// ExamHistoryGroupBy is the group-by builder for ExamHistory entities.
type ExamHistoryGroupBy struct {
	selector
	build *ExamHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ehgb *ExamHistoryGroupBy) Aggregate(fns ...AggregateFunc) *ExamHistoryGroupBy {
	ehgb.fns = append(ehgb.fns, fns...)
	return ehgb
}

// Scan applies the selector query and scans the result into the given value.
func (ehgb *ExamHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ehgb.build.ctx, "GroupBy")
	if err := ehgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamHistoryQuery, *ExamHistoryGroupBy](ctx, ehgb.build, ehgb, ehgb.build.inters, v)
}

func (ehgb *ExamHistoryGroupBy) sqlScan(ctx context.Context, root *ExamHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ehgb.fns))
	for _, fn := range ehgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ehgb.flds)+len(ehgb.fns))
		for _, f := range *ehgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ehgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ehgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExamHistorySelect is the builder for selecting fields of ExamHistory entities.
type ExamHistorySelect struct {
	*ExamHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ehs *ExamHistorySelect) Aggregate(fns ...AggregateFunc) *ExamHistorySelect {
	ehs.fns = append(ehs.fns, fns...)
	return ehs
}

// Scan applies the selector query and scans the result into the given value.
func (ehs *ExamHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ehs.ctx, "Select")
	if err := ehs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamHistoryQuery, *ExamHistorySelect](ctx, ehs.ExamHistoryQuery, ehs, ehs.inters, v)
}

func (ehs *ExamHistorySelect) sqlScan(ctx context.Context, root *ExamHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ehs.fns))
	for _, fn := range ehs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ehs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ehs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
